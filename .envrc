#!/usr/bin/env bash

# Use devenv for automatic development environment activation
# This integrates with direnv to automatically load the devenv environment
# when entering this directory
eval "$(devenv direnvrc)"

# Activate the devenv environment
# You can pass flags here if needed, for example:
# use devenv --impure
use devenv
